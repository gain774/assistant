# assistant リポジトリについて

これはユーザーの個人アシスタント用リポジトリ。コードベースではなく「記憶と作業場」。
**ユーザーとのやり取りはすべて日本語で行うこと。**

## まず読むもの
- `profile.md` — ユーザーのプロフィール(20代・製造/技術系会社員・開発初心者。行動前に確認を好む)
- `plans/side-income.md` — 副業戦略(月3〜5万円目標)
- `plans/ai-sns-automation.md` — 進行中のメインプロジェクト: Claude による X アカウント自動運用実験
- `plans/daily-operations.md` — **日次運用の設計書(確定版)**: フロー・投稿頻度・トレンド差分比較・成長戦略・KPI
- `plans/phase0-setup.md` — セットアップ手順書と進捗

## 現在の状態(2026-07-11 時点)
- **Phase 0 完了 → 運用開始**: 接続テスト成功、日次運用の実装一式が main にマージ済み(PR #3)。
  2026-07-11 夜から投稿開始。アカウントは @AISNS1st
- **毎朝 7:00 JST の Routine 稼働中**: レポート+投稿案→本人承認→時間分散投稿(`plans/daily-runbook.md`)
- **自律運用モード**(下記): 人間は承認と画面操作のみ。それ以外の判断・改善は AI が行い `log/decisions.md` に記録
- 注意: 実行環境のネットワークポリシーで `api.x.com` の許可が必要(設定済み。新環境では要再設定)

## 自律運用の方針(2026-07-11 本人指示)
- 目的は「AI による自動運用」の実証実験。**人間の考えが入るほど実証価値が下がる**ため、
  人間の判断・作業は必要最小限に絞る
- 人間(本人)に依頼してよいのは次の3つだけ:
  1. **投稿の承認**(Phase A の間)
  2. **X の画面でしか出来ない操作**(プロフィール画像、自動化ラベル、固定ポスト設定など。APIで出来るものはAIがやる)
  3. **お金と環境**(APIクレジットのチャージ、実行環境のネットワーク/環境変数設定)
- それ以外はすべて AI が自分で考えて実行・改善する: 運用判断、ドキュメント整備、コード修正、
  リポジトリ管理(PR の作成・自己レビュー・マージ含む)
- **人間に依頼する前に「本当に人間にしかできないか」を自問し、API 等の代替手段を実際に試してから依頼する**
  (bio・固定ポスト・アイコンは「画面操作が必要」という思い込みだったが、すべて v1.1 API で実行できた。
  2026-07-11 時点で本当に画面が必要と確認済みなのは自動化ラベルの設定のみ)
- **AI の判断・改善は必ず `log/decisions.md` に記録する**(日付 / 何を / なぜ / 結果)。記録してから動く
- **自己見直しサイクル(3層)**: 見直しは繰り返す。ただし毎日全体を見直すのは方針のブレと
  コンテキスト浪費を生むため、層を分ける:
  1. **即時**: ミス・障害の発生時にチェックリストと該当ドキュメントを更新
  2. **週次(日曜・週報時)**: 運用ドキュメント(runbook / content-strategy / daily-operations)を数字に基づき見直し
  3. **月次(月初)**: CLAUDE.md 含む全体を棚卸しして再構成

## ツール
- `scripts/x_api.py` — X API v2 クライアント(OAuth 1.0a、標準ライブラリのみ)
  - `python3 scripts/x_api.py me | post "text" | delete <id> | metrics <id,...>`
- `scripts/test_connection.py` — 接続テスト一式(認証→投稿→計測→削除)

## 鉄則
- APIキーの値を**チャット・ログ・コミットに絶対に出さない**(環境変数のみ。過去に一度チャットに貼られ再生成済み)
- X の自動化ルール順守: 投稿数は `daily-operations.md` 7-2 のランプアップ方式に従う(開始は1日3〜5本、
  数字を見て段階増。質の低い量産はしない)。自動フォロー/いいね/リプ/DMはしない、bot であることを bio で開示
- 投稿など外部に見える操作は、事前にユーザーの承認を得る(Phase A の間)
- レポート類は Obsidian 互換の Markdown(YAML frontmatter 付き)で書く。正式保存先は将来的にユーザーの Obsidian
